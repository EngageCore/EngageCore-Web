version: '3.8'

services:
  engagecore-web:
    image: engagecore-web:latest
    container_name: engagecore-web
    ports:
      - "5173:5173"
    restart: unless-stopped
    networks:
      - engagecore-network

networks:
  engagecore-network:
    driver: bridge

